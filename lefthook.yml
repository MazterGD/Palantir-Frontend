pre-commit:
  # Run ESLint and Prettier before committing
  eslint:
    run: "npx eslint 'src/**/*.{js,ts,tsx}'"
  prettier:
    run: "npx prettier --check 'src/**/*.{js,ts,tsx,css,scss,md}'"
  typescript:
    run: "npx tsc --noEmit"
  parallel: true

pre-push:
  build:
    run: "npm run build"
